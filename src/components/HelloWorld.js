import styled from "styled-components";

const HelloWorldStyle = styled.h1`
font-weight: bold;
`;

const HelloWorld = () => {
  return (
    <HelloWorldStyle>HelloWorld</HelloWorldStyle>
  )
}

export default HelloWorld;